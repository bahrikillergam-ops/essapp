services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: essteam
      POSTGRES_PASSWORD: password
      POSTGRES_DB: essteam_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: